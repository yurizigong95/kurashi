# くらしの手帳

支払い・履修・時間割・予定・バイト・相談をひとつにまとめた、自分用のアプリです。
GitHub Pages（https://yurizigong95.github.io/kurashi/ ）で動かします。

## もう1つのアプリ：もんだいメーカー（`study/`）

授業の資料から問題を作って解く勉強アプリを、**別のアプリ**として `study/` に置いています。
くらしの手帳とはコードも保存場所も別で、1つで完結します。

- 開く： https://yurizigong95.github.io/kurashi/study/
- くわしくは `study/README.md`

## ファイルの中身

| 場所 | 中身 |
|---|---|
| `index.html` | 画面の骨組み（ここから下のファイルを読みこむ） |
| `css/app.css` | 見た目 |
| `js/data.js` | 科目・時刻表・履修のデータ |
| `js/core.js` | 共通の道具・保存・取り消す・エラーの記録 |
| `js/sync.js` | 端末どうしの同期（写真もふくむ） |
| `js/ai.js` | AI（Gemini）を呼ぶ道具 |
| `js/google.js` | Googleカレンダー・Googleドライブとの連携 |
| `js/*.js`（そのほか） | 各画面（今日・時間割・予定・お金・そうだん など） |
| `js/chara-*.js` / `js/pet.js` | キャラクター・セリフ・おせわ（育てる） |
| `js/kakeibo.js` | 家計簿（CSV・Apple Payの記録） |
| `js/links.js` / `js/notify.js` | iPhoneのショートカット・ウィジェット・Google ToDo・通知 |
| `js/ai-plus.js` | AIの直接登録・ネット検索・会話モード・読み上げ・週のふりかえり |
| `js/wx-plus.js` | 気象庁の警報・雨雲レーダー |
| `js/ops.js` | エラーの自動送信・表示の速さ・アップロードの確認・前の版にもどす |
| `js/hooks.js` | 足した機能の差しこみ口（画面・操作・設定・ページの枠・通知・検索・点検・AIの道具）と「勉強」タブ |
| `js/aidata.js` | AIそうだんが手帳のすべてを調べる道具（目次・分野ごとに読む・ことばでさがす。カギは見せない） |
| `js/m-kokushi.js` | 勉強①：国試ドリル・略語辞書（Wikipedia）・基準値・薬のカード・解剖図の穴うめ・看護技術の手順・出典 |
| `js/m-research.js` | 勉強②：看護過程・看護計画・レポート・参考文献・論文と本さがし・本だな・講義の録音・翻訳・法令 |
| `js/m-anki2.js` | 暗記の追加：テスト範囲の逆算・耳で暗記・ひっかけ問題・まちがえたカードの解説・Ankiと交換 |
| `js/m-campus.js` | 授業・バイト・通学：出席率・大学のメール・シラバス・成績の見込み・GPA・学年暦・間に合う？・経路・シフト |
| `js/m-life.js` | 生活・健康：睡眠・歩数・周期・予防接種・寒暖差/花粉/暑さ指数・今日の作戦・声/写真で登録・週の分析 |
| `js/m-petplus.js` | おせわの追加：着せかえ・進化の分かれ道・図鑑・もようがえ・おさんぽ・天気・記念日 |
| `js/m-chara2.js` | キャラの追加：性格と口調・毎週のAIのセリフ・なかよし度・声で話す・キャラどうしの会話 |
| `js/m-links2.js` | iPhone・連携：Googleカレンダー・リマインダー・ウィジェット・Siri・Watch・バッジ・ics・Discord・手書きノート・BGM・祝日 |
| `js/m-core2.js` | 全体：アプリ全体の検索・文字の大きさ・ホーム画面とタブ・データの点検・前の日の自動評価 |
| `css/m-*.css` | 上の足した機能の見た目 |
| `gas/Code.gs` | Google連携の「橋わたし」（Apps Script に貼るプログラム） |
| `gas/appsscript.json` | 橋わたしの設定ファイル（Apps Script のエディタに貼る） |
| `files.json` | アップロードの確認に使うファイルの一覧（`tools/チェック.bat` で作る） |
| `tools/` | 公開（`公開.bat`）・アップロード前のファイル確認・GitHubでの自動テストの道具 |
| `.github/workflows/ci.yml` | GitHubにアップロードしたときの自動テスト |
| `tests/` | 自動テスト（本物のデータには触れません） |
| `sw.js` / `manifest.json` / `icon-*.png` | ホーム画面に置いたときのための設定 |

`js/` のファイルは、`index.html` に書いた順番で読みこみます。順番は変えないでください。

## 公開のしかた（GitHub に上げる）

このフォルダは GitHub の `kurashi` リポジトリとつながっています。

- **`tools/公開.bat` をダブルクリック**するだけで、ファイルの確認 → GitHub へのアップロードまで自動でします
  （Claude に「公開して」と頼んでも同じです）
- 1〜3分で https://yurizigong95.github.io/kurashi/ に反映されます。スマホ・iPad・パソコンでアプリを開き直してください
- 設定 › 端末どうしの同期 で、どの端末も同じ版（`APP_BUILD`）になっているか確かめられます
- はじめての時だけ、ブラウザで GitHub のログインが出ます（そのあとは覚えています）
- アップロードのたびに、GitHubの「Actions」タブで自動テスト・ファイルの抜け・表示の速さ（Lighthouse）が動きます

GitHub のページから「Upload files」で上げるのは、なるべくやめてください（パソコンの中身とずれて、`公開.bat` が失敗します）。

アプリのファイルが足りないときは、アプリが「アプリを開けませんでした」と原因を出します。
古いファイルが残っているときは、その画面の「しまってある古いファイルを消して読みこむ」を押してください（予定などのデータは消えません）。

## テストのしかた

パソコンで `tests/index.html` を開くと、2台の端末をまねて自動でたしかめます
（`https://yurizigong95.github.io/kurashi/tests/` でも動きます）。
テストは「テストモード」で動くので、本物のデータ・Firebase・Googleには触れません。
足した機能のテストは `tests/tests-m-*.js`、ぜんぶを合わせた点検は `tests/tests-final.js` にあります（`?only=ことば` で、名前にそのことばがあるテストだけを動かせます）。
アプリ本体も、URLに `?test=1` をつけるとテストモードになります。

## Google連携（カレンダー・ドライブ・ToDo・通知・ショートカット）のつなぎ方

アプリの 設定 › Google連携 に、同じ手順が出ます。

1. 「合言葉を作って、プログラムをコピー」を押す
2. https://script.google.com で「新しいプロジェクト」→ Code.gs の中身をぜんぶ消して貼り付け → 保存
3. 左の「プロジェクトの設定」→「appsscript.json マニフェスト ファイルをエディタで表示する」にチェック → `appsscript.json` を「設定ファイルをコピー」の中身に置きかえて保存
4. 「デプロイ」→「新しいデプロイ」→ 種類「ウェブアプリ」
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**
5. 「デプロイ」→ Googleの確認で許可（「このアプリは確認されていません」→「詳細」→「移動」）
6. 出てきた「ウェブアプリのURL」をアプリに貼って「保存して、つながるか試す」（5分ごとの確認も自動で動きます）

### スマホへの通知

1. Firebase コンソール › プロジェクトの設定 › Cloud Messaging › ウェブプッシュ証明書 で「鍵ペアを生成」
2. その鍵をアプリの 設定 › 通知 に貼って保存
3. iPhoneは **ホーム画面に追加したアプリ** から「この端末で通知を受け取る」（iOS 16.4以降）
4. テストが届かないときは、Firebase の「サービスアカウント」で作った秘密鍵（JSON）の中身を、Apps Script の「プロジェクトの設定 › スクリプト プロパティ」に `SA_JSON` という名前で入れると、そちらの権限で送ります

- 予定は Googleカレンダーの「くらしの手帳」というカレンダーに入ります。通知は **前日の0時** です。
- バックアップは Googleドライブの「くらしの手帳バックアップ」フォルダに、週に1回（新しい12回分）入ります。
- 合言葉とURLは端末ごとに保存されます（同期はしません）。1台だけつないでおけば、ほかの端末の変更もその端末から送られます。
- プログラムを直したときは、「デプロイを管理」→ 編集 → 新しいバージョン で入れ直してください。

## Firebase のルール（念のため確認）

Firebase コンソール › Firestore Database › ルール が「テストモード（期限つき）」のままだと、
期限が来た日から同期が止まります。次のようにしておくと、期限なしで、このアプリの文書だけを使えます。

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /shiharai/{doc} {
      allow read, write: if doc.matches('8b7f4e6et9jhxded.*');
    }
  }
}
```

※ このやり方では、部屋のID（上の英数字）を知っている人は読み書きできます。
部屋のIDはアプリのファイルの中に書いてあるので、より安全にするには、同期するデータの暗号化かログインのしくみが必要です。
